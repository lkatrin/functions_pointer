/*Описать функцию MinElem(A, N) целого типа, находящую минимальный элемент целочисленного массива A размера N.
С помощью этой функции найти минимальные элементы массивов A, B, C размера NA, NB, NC соответственно*/

//https://coolcode.ru/reshaem-zadachi-abramyan-na-paskale-param1
//https://younglinux.info/c/array-function
#include <stdio.h>
void MinElem(int A[],)
{
    int MinEl;
    MinEl=1;
    for (int i=1; i<N)
    {
        if (A[i] < A[MinEl])
        MinEl = [i];
    }
    return MinEl;
}

int main()
{
    int A[],B[],C[];
    int Na,Nb,Nc,N;
    
    scanf("%d",&N);
    for(int i=1; i<N; i++)
    scanf("%d",&A[i]);
    
    
}
